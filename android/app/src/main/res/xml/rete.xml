<?xml version="1.0" encoding="utf-8"?>
<!--
  Il computer di casa parla in HTTP semplice, e va bene così: il traffico non
  esce dalla tua rete. Android dalla versione 9 vieta l'HTTP per impostazione
  predefinita — giustamente, perché su Internet è indifendibile — e qui si fa
  l'eccezione **solo** per gli indirizzi privati.

  Un indirizzo pubblico resta vietato: se un giorno qualcuno provasse a
  puntare l'app fuori dalla rete di casa, lo farebbe in chiaro, e questa riga
  glielo impedisce.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
